    git pull origin main

çakışma varsa:
    git stash
    git pull origin main
    git stash pop

.so dosyasını derleme:
    g++ -O3 -ffast-math -shared -fPIC -o lib_integrator.so integrator.cpp
    g++ -O3 -ffast-math -dynamiclib -o integrator.dylib integrator.cpp

yüklememe konusunda çok ısrarcı ise:
    git push origin main --force

sürüm oluşturma:
    git add .
    git commit -m "duz bolge, tune'lar ve rf duzeltildi"
    git tag -a v2.0 -m "demet ve spin dinamigi tamam gibi gorunuyor."
    git push origin v2.0

release oluşturma:
    gh release create v2.0 --title "Demet ve Spin Dinamiği Sürümü" --notes "Düz bölge, tune'lar ve RF düzeltmeleri eklendi."
